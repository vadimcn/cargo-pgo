extern crate cmake;
use std::env;

fn main() {
    // cmake::Config::new("llvm")
    //              .profile("Release")
    //              .build_target("llvm-profdata")
    //              .build();
    // println!("cargo:warning={:?}", env::var("CARGO_MANIFEST_DIR")); 
}